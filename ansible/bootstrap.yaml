---
- name: Bootstrap VPS base packages
  hosts: vps
  become: true
  gather_facts: false

  tasks:
    - name: Refresh apt cache and upgrade installed packages
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
        upgrade: dist
        autoremove: true
        autoclean: true
